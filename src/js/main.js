(

  function init() {

    $(document).ready(function(){
      // Sticky nav
      $('.nav-wrapper').sticky({topSpacing:0});
      // smooth scroll
      $('.nav-wrapper a').smoothScroll();
    });

  }

)();
